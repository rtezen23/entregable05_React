import React from 'react';

const Pagination = ({}) => {
    return (
        <div>
            <button>Previous</button>
            <button>Next</button>
        </div>
    );
};

export default Pagination;